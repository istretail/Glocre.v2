export interface ApiSignature {
    sign(url: string, date: string, method: string, body: string, contentType: string, headers: any): string;
}
export declare class ApiSignatureImplement implements ApiSignature {
    private readonly apiSecret;
    constructor(apiSecret: string);
    sign(url: string, date: string, method: string, body: string, contentType: string, headers: any): string;
    private canonicalizedHeaders;
    private canonicalizedResource;
    private signString;
}
//# sourceMappingURL=signature.d.ts.map